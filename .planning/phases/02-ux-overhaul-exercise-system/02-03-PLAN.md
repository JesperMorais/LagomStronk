# Plan 02-03: Volume Chart with Gradient

## Metadata
```yaml
wave: 2
depends_on: [02-01]
files_modified:
  - js/app.js
  - css/style.css
autonomous: true
```

## Goal
Add stunning volume chart to dashboard showing this week's daily volume with gradient bars.

## Context
User wants: "Volume chart: bar chart with stunning options - this week's daily volume"
Chart.js already included. Use gradient from mint to transparent.

## Tasks

<task id="1">
Add canvas element to hero section for volume chart:
- Small chart area in the stats section
- Responsive sizing
</task>

<task id="2">
Create initVolumeChart() function:
- Get canvas context
- Create linear gradient (mint to transparent)
- Configure Chart.js bar chart with gradient fill
- Round bar corners (borderRadius: 8)
- Hide legend, minimal grid
</task>

<task id="3">
Add getWeekVolumeData() function:
- Calculate volume (weight × reps) per day Mon-Sun
- Return array of 7 values
</task>

<task id="4">
Integrate chart with renderHero():
- Initialize chart on first render
- Update data on subsequent renders
- Handle empty state (no workouts this week)
</task>

<task id="5">
TEST: Add a workout with sets, verify:
- Chart shows bar for that day
- Gradient looks good (mint fading down)
- Chart updates when sets are logged
- Mobile responsive
</task>

## Verification
```bash
grep -q "initVolumeChart\|getWeekVolumeData" js/app.js && echo "✓ Volume chart functions exist"
```

## Must Haves
- [ ] Volume chart displays on dashboard
- [ ] Gradient effect visible (mint to transparent)
- [ ] Chart shows correct days (Mon-Sun)
- [ ] Empty state handled gracefully

# Plan 02-07: Workout History Calendar

## Metadata
```yaml
wave: 3
depends_on: []
files_modified:
  - js/app.js
  - css/style.css
  - index.html
autonomous: true
```

## Goal
Replace history list with calendar view showing workout intensity gradient.

## Context
User wants:
- Month overview with expandable weeks
- Day markers with intensity gradient (volume-based)
- Tap day for quick summary popup

## Tasks

<task id="1">
Add calendar HTML structure:
- Month navigation (< Month Year >)
- Day headers (M T W T F S S)
- Calendar grid (6 weeks × 7 days)
- Day summary popup (hidden by default)
</task>

<task id="2">
Add calendar CSS:
- Grid layout for days
- Day circles with gradient backgrounds
- Intensity colors: transparent (none) to full mint (high volume)
- Popup styling with workout summary
- Current day highlight
</task>

<task id="3">
Add calendar functions to app.js:
- renderHistoryCalendar() - renders current month
- calculateDayIntensity(date) - 0-1 based on volume
- getIntensityColor(intensity) - returns rgba mint color
- navigateMonth(delta) - prev/next month
</task>

<task id="4">
Add day tap interaction:
- Show popup with workout summary
- Display exercises and total sets
- Tap outside to close popup
- Tap day with no workout shows "Rest day"
</task>

<task id="5">
TEST: Go to History view, verify:
- Calendar displays current month
- Days with workouts have color intensity
- Month navigation works
- Tapping a day shows popup with summary
</task>

## Verification
```bash
grep -q "calendar" css/style.css && echo "✓ Calendar CSS exists"
grep -q "renderHistoryCalendar" js/app.js && echo "✓ Calendar JS exists"
```

## Must Haves
- [ ] Calendar view displays correctly
- [ ] Intensity gradient shows workout volume
- [ ] Month navigation works
- [ ] Day tap shows summary popup
